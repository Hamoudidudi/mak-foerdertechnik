# =========================
# === SMTP (E-Mail-Versand)
# =========================
# Trage HIER später deinen echten SMTP-Anbieter ein (Beispiele siehe unten).
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false                # true NUR bei Port 465
SMTP_USER=DEIN_SMTP_LOGIN        # z.B. postfach@deine-domain.tld
SMTP_PASS=DEIN_SMTP_PASSWORT
SMTP_FROM="MAK Fördertechnik <no-reply@deine-domain.tld>"
SMTP_TO="mohamad.tammae@gmail.com"

# =========================
# === reCAPTCHA v3
# =========================
# Für LOKALEN Test NICHT nötig. Wir mocken die Prüfung.
# (Wenn du später echte Keys hast, hier eintragen)
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=
RECAPTCHA_SECRET=

# =========================
# === DEV-Helfer
# =========================
# reCAPTCHA lokal überspringen (immer true lassen, bis du echte Keys nutzt)
MOCK_RECAPTCHA=true

# Umgebung
APP_ENV=development


MAIL_DRY_RUN=true